server:
  port: 18080

# 回写 SQL 接口（默认从 spring.datasource.url 自动推导 arap 服务地址）
writeback-sql:
  tenant-id: 3XNLNA60G34006P
# 表达式引擎配置
expression-engine:
  local-writeback-sql: false  # true=本地生成回写SQL, false=HTTP远程调用(默认)
# 决策表分析配置
decision-table:
  scan-path: /Users/pengfyu/workspace/feature/dev  # opus77 代码库根目录，用于扫描 CSV 决策表
#连接数据库
spring:
  mvc:
    async:
      request-timeout: -1   # 关闭 Tomcat async 超时，流式响应按实际耗时完成
  neo4j:
    uri: bolt://localhost:7687
    authentication:
      username: neo4j
      password: ypfect93
  datasource:
#    url: jdbc:postgresql://localhost:5432/testapp
    url: jdbc:postgresql://postgres.test-tx-16.e7link.com:5432/tenant-public
#    url: jdbc:postgresql://postgres.release-tx-1.e7link.com:5432/tenant-public
    password: stet44{czars
#    password: 123
    username: postgres
    driver-class-name: org.postgresql.Driver
  jackson:
    serialization:
      INDENT_OUTPUT: true
mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    root: INFO
    com.deepmodel.relation: DEBUG

# 连接外部独立的 Neo4j 图数据库配置已合并至上方


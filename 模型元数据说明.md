---




title: "untitled"
date: 2026-01-09
type: knowledge
project: general
workspace: unknown
source: manual
tags: [custom, knowledge]
status: active
summary: "[toc]"
inbound_links: 0
related:
  - "[[2026-04-22-[unknown]-GEMINI.md 提示词优化方案|GEMINI.md 提示词优化方案]]"
  - "[[调试快速参考|SQL连接信息调试 - 快速参考卡]]"
  - "[[核销框架详细分析|核销框架详细分析]]"
  - "[[核销系统深度分析总结|核销系统深度分析总结]]"
  - "[[2026-04-20-[feature]-回写后校验（Validation After Wr|回写后校验（Validation After Wr]]"
---
[toc]

#1 基本概念

**模型元数据**： 主要用于建立数据模型的元数据，包括实体对象的定义，实体对象的属性构成，以及对象之间的关联关系。其中对象之间的关系主要用于构成对象的之间的网络，以便生成GraphQL Schema。<mark>注意模型元数据虽然有些属性可以用于UI前端，但不描述UI相关的特性。</mark>

**实体(Entity)**：即数据对象，与现实中理解的对象对应，不对实体进行抽象，但可以将具备某些相同特性的实体归纳为实体具有的特性，特性表示实体具有类似的属性的操作逻辑，比如树状实体，都有treePath, treeLevel, parent等属性，在保存时都能够自动完成这些值的处理。

**实体属性**: 表示构成实体的特征信息，普通的属性与数据库的字段有对应关系。某些特殊的属性不与数据库字段对应，但描述了实体之间的关系，如外键对象属性， 相关对象列表属性等。由于实体属性通常与数据库字段对应，有时说把属性也称为字段。

**实体间关系**： 现实世界的实体之间通常存在关联关系，这种关联关系把实体连接成一个网络，数据之间的联系、联查也通过该关系网络进行。对于应用系统而言，目前实体间的关系有以下几类：

  - **参照关系**： 也叫外键关系， 由一个实体的外键字段指向另一个实体的ID, 参照关系为照参源对象角度，是一对一的关系。从被参照的对象角度，是多对一关系。参照关系体现在实体对象上时，即参照对象上除了一个外键ID字段外，还有一个指向被参照对象的对象属性；同样被参照对象上可以有一个List类型的属性，表示关联的对象列表。
  - **多态参照关系**： 即一个对象参照的实体对象不确定，表现为多种形态。多态参照描述的是从属关系，即一个对象可以归属到其他多个对象上。比如附件可以归属于客户对象，也可以归属于订单对象。多态参照由一个ID字段和一个type字段构成。ID字段类似于外键关系中的ID, 但ID的值不只是一种对象的ID, 具体是哪个对象的ID, 由type字段说明，比如type字段值是Customer，则ID的值为客户的ID。 因此多态参照对于一个具体的记录来说，参照关系是明确的，但整个对象的参照关系是多态的。多态参照体现的实体对象上，通常只是在被参照对象上可以有一个List类型的字段表示关联的参照对象列表，如果需要也可以在参照对象上为每一个被参照对象生成一个指向该对象的属性。
	目前对多态参照的情况，如果参照的对象定义了dataMappings信息，则会将参照对象基本信息通过trigger写入到ObjectData表中， 这样多态参考对象可以用gql直接查询该多态参照对象的基本字段信息。以下情况将自动定义mapping:
	- document类型的Entity: 如果有code、name, isDisabled字段，则自动映射，不需要定义，只在这个范围外的需要定义即可。(其中code长度不能超过64，name长度不能超过256）
	- bill类型的Entity, 如果isDetail=false, 则自动映射code, businessDate, billTypeId, businessTypeId和approveStatus等
	- detailRecord类型的Entity, 则自动映射code和businessDate
  
  - **反向参照关系**： 反向参照关系是相对于参照有关系和多态参照关系而言的，而且是在存在参照关系或者多态参照关系的情况下，才可能有反向参照关系。反向的意思是，如果从左到中是参照关系，哪么从右到左，通常是1：n关系， 但在指定的条件下，从右到左可以明确限定为1:1关系，这种1:1关系就向普通参照关系一样存在，只是有限定条件而已。比如联系人指向客户的参照关系中，如果联系人有一个标识为主联系人的字段isPrimary, 则在isPrimary=true时， 相似于客户上有一个外键属性主联系人，指向对应的联系人对象。反向参照如果定义了，可以用于对象之间的联查。

**领域应用**： 由于系统可能非常复杂，为了能够简单，采用微服务的模式，将复杂的系统分解为不同的领域应用，通常领域应用之间的操作是隔离的，如果领域有依赖关系，因此允许只读访问依赖的领域应用的服务接口。对于某些特别的对象，比如附件和评论等，可以允许直接访问CUD服务接口，对应的CUD操作实际上还是发生在当前的领域应用范围，只是操作的对象是依赖的领域，还是在当前领域操作的一个事务范围内。

    >注意：如果一个领域应用调用另一个领域的服务，不是通过rpc方式调用，而是直接通过依赖的api java接口调用，实际是走走了服务的标准逻辑，如果被调用的服务有特殊的业务逻辑，将不触发。原因是依赖的api中的服务接口是自动生成的，特别的逻辑是在应用的实现中Override重写的。因此尤其是对于CUD操作，一定要确保逻辑上没有问题。
  
#2 模型元数据基本规范
**元数据分领域应用**： 模型元数据主要描述的是数据模型，由于系统采用微服务的模式划分为领域应用，相应元数据也按领域应用划分，每个领域应用仅定义自己领域的对象元数据。
**领域元数据之间有依赖关系**： 由于对象可能存在共享，比如档案类对象，因此允许领域之间有依赖关系，相应的元数据也存在依赖关系，即本领域可以引用依赖的其他领域的元数据，但无法改变其它领域的元数据。
**公共的元数据统一标准**：对元数据字段类型，标准的实体特性，系统字段统一标准，统一进行定义。 

##2.1 字段数据类型
实体的字段是构成实体的主要元素，它通过不同的数据类型来描述实体的特征。为了保持数据类型的简单和易于交换，字段的类型只描述数据本质的数据类型，不描述属性的业务含义（即不描述字段的业务类型），比如手机号和邮件地址，在数据类型上都是字符型，但模型元数据并不描述电话类型和邮件类型，如果有需要，可以通过增加业务类型的方式来描述字段的业务含义。目前的字段数据类型仅支持如下的类型：
	
类型 | 类型名称 | 说明
----|-------- | ----
String | 字符型 | 字符型通常需要指定长度，如果不指定，表示不限制长度。当用于ID字段时，不需要指定长度，所有的ID类型默认长度为64bytes。
Long | 长整型 | 不需要指定长度 
Integer | 整型 | 不需要指定长度
BigDecimal | 数值型 | 不需要指定长度，默认对应的字段精度为（24，8）
Timestamp | 日期时间型 | 用于日期和时间，不需要指定长度
Boolean | 布尔型 | 不需要指定长度
JsonElement | Json类型，对应数据库的jsonb字段，可以保存json对象或者json数组。
JsonObject | Json对象类型，对应数据库的jsonb字段，只能保存json对象，所有的自定义字段保存在一个该类型的字段中
List | 列表类型 | 描述对象具有的列表对象，不对应数据库字段，通常List属性是通过外键类型的字段自动生成，不需要定义，但在跨域的情况下，需要人工定义。有关跨域的对数据类型的影响参考后面的字段元数据定义。
 
##2.2 标准系统字段/属性
标准字段是每个实体对象都具有，并且能够自动赋值的字段，包括：
 
字段名称 | 字段类型 | 有无对应DB字段 | 说明
----|-------- | ---- | ----
id | String | | 对象主键ID字段，64bytes长度
createdUserId | String | | 创建人ID
createdUser | User对象 | 无 | 创建人引用对象
createdTime | Timestamp | | 创建时间
modifiedUserId | String | | 修改人ID
modifiedUser | User对象 | 无 | 修改人引用对象
modifiedTime | Timestamp | | 修改时间
isSystem | Boolean | | 是否为系统数据, 通常系统数据不允许删除
isInitData | Boolean | | 是否为预置数据
isDeleted | Boolean | | 是否被逻辑删除，或者禁用了初始数据。目前对初始数据采取的是copyOnWrite的方式，因此如果用户修改了初始数据，实际上是自动复制一条一样的数据，把原初始数据标识为删除，并且ID增加后缀-disabled, 新增加的记录保持原ID。对于普通数据的删除，则不需要修改ID
dataVersion | Long | | 数据版本，每次更改会自动加1， 用于并发时的乐观锁机制
customizedFields | JsonObject | | 自定义字段值，保存了该对象的所有自定义字段值
toNullFields | Set&lt;String> | 无 | 需要清空的字段，主要用于更改一个对象时，由于mybatis采取的是updateBySelective, 因此要清空一个字段的值，必须将字段名放到toNullFields中
additionalValues | Map&lt;String,Object> | 无 | 其它未知字段值，主要用于提交数据时，如果有附加的值需要传递到后端处理，可以使用该字段，同时对于提交的数据中，有未知的字段，也会放在这里
customizedListFields | Map&lt;String,Object> | 无 | 自定义列表字段，用于保存自定义外键字段对应的list字段数据，KEY为List字段名，值为数据列表
customizedTableName | String | 无 | 自定义表名，用于自定义数据提交时，需要标明自定义对象的表名, 由service层自动赋值，在mybatis层使用该字段
	
##2.3 实体的特性字段
实体的特性字段，是指但实体具备某一特性时具有的字段，通常这些字段的值也会自动生成。目前的支持两类特性：树状特性和子表特性。

###2.3.1 树状特性
树状特性是指实体自身存在上下级关系，形成一棵树，它具有的字段有：
 
字段名称 | 字段类型 | 有无对应DB字段 | 说明
----|-------- | ---- | ----
parentId | String | | 上级ID, 如果没有上级，值为null
parent | Entity | 无 | 上级引用对象
idPath | String | | 由上级对象ID加逗号构成的路径，eg.: 1,2, 自动赋值
level  | Integer | | 级次，从0开始，自动赋值
isLeaf | Boolean | | 是否叶级，自动赋值
	
###2.3.2 子表特性
子表特性是指该对象做为一个子表存在，子表通常有共同的特性，它具有的字段有：

字段名称 | 字段类型 | 有无对应DB字段 | 说明
----|-------- | ---- | ----
ordinal | Integer | | 顺序号
editFlag | EditFlag | 无 | 编辑标志，主要用于修改对象时，取值为：add, update, delete, old
 
#3 元数据定义
元数据采用json对象描述，分为Entity元数据和Enum元数据，其中Entity元数据包括了字段元数据定义，字段元数据又包含了关系的定义。Enum元数据包括枚举类型和枚举值的定义。元数据的json格式采取json schema定义，参考 metadata-api/schemas下的json schema文件，metadata-api工程，通过json schema生成了元数据定义对象的接口，然后再使用Immutables定义，生成了对应的Immutable 实现。 
##3.1 实体元数据定义
<style>table th:nth-of-type(3) { width: 120px; }</style>

实体元数据构成说明如下:

属性 | 类型(json) | 中文名称 | 必填 | 说明
----|----|---- | ---- | ----
name | string | 对象名称 | 是 | 要求全局唯一，以大写字母开始，最长32 bytes。 为UpperCamel格式，对应的表名为LowerUnderscore格式（加上域应用名前缀），如SalesOrder （对应表名为sales_sales_order)
title | string | 对象的显示名称 | 是 | 主要是中文显示名称，多语支持目前暂不考虑
desc | string | 对象的说明文字 | 否
appName | string | 所属应用 | 否 | 不需要填写，是在项目build时，自动设置
isDetail | boolean | 是否为明细对象 | 否 | 设置为true表示为明细对象,明细对象通常不会独立保存，因此不会生成服务接口, 同时明细对象会生成明细特性的字段（如ordinal)，参考子表特性说明。 明细对象实际要依赖于一个主对象，因此在明细对象上有一外键字段指向主对象。默认值为false
isTree | boolean | 是否为树型对象 | 否 | 如果是一个树型对象，则自动生成parentId, idPath, level, isLeaf等字段，参考树状特性说明。默认值为false
isPhysicalDeletion | boolean | 是否为物理删除 | 否 | 如果是物理删除，对应的子表也需要是物理删除，默认为false
isDisableDataScope | boolean | 不需要数据权限 | 否 | 表示操作该对象时，不需要数据权限，默认为false
isCustomizedEntity | boolean | 是否为自定义对象 | 否 | 自定义对象，该值为true
isMultiDataVersion | boolean | 是否支持数据多版本 | 否 |  是否支持数据多版本，默认为否
  isSupportChangeLog | boolean | 是否支持变更单 | 否 | 如果为支持时，将处理变更单
type | string | 对象类型 | 否 | 标明对象的类型，可以取值为setting, document和bill等
eventTo | Integer | 发送消息 |   |  是否发送消息到1:流程服务,2:ETCD,4:NSQ, 或者3者的组合，不指定按默认规则发送 
businessModuleId | String | 对象所属模块 |   |  类型为bill时，必填， 因为要根据这个模块生成对应的单据类型配置信息， 该属性的取值为 BusinessModule 的枚举值一致
fields | array | 字段定义 | 是 | 对象支持的字段列表，具体的元素值参考字段元数据定义
indexes  | IndexDef | 索引定义 | 否 | 定义实体的索引
dataMappings | object | 数据映射定义 | 否 | 要将数据同步到ObjectData表时，需要有字段映射，如果不指定，按默认的规则映射
viewDef  | ViewDef | 视图定义 | 否 | 定义实体的索引, 如果有视图定义，要求其他 isXxx特性不应该有（默认为false)
properties | object | 对象的其他属性 | 否 | 对象的其他元数据说明，为一个map结构，根据具体的场景填写值, 具体支持的proerties定义，参考 EntityProperties.java中的定义
checkRules | List<CheckRule> | 业务检查规则定义 |  使用保存数据保存时，如果不符合业务规则，则错，详情查看CheckRule的定义。 它可以结合回写表达式，完成一些回写后也需要满足的规则定义
	
##3.2 字段元数据定义
###3.2.1 字段元数据构成
属性 | 类型(json) | 中文名称 | 必填 | 说明
----|----|---- | ---- | ----
name | string | 字段名称 | 是 | 要求本对象中唯一，以小写字母开始，最长32 bytes。 为LowerCamel格式，对应的DB中字段名为LowerUnderscore格式），如customerId （对应DB字段名为customer_id)
title | string | 字段的显示名称 | 是 | 主要是中文显示名称，多语支持目前暂不考虑
desc | string | 字段的说明文字 | 否
type | string | 字段的类型 | 是 | 支持的类型参考 2.1 字段数据类型。如果是字符型，需要指定长度，否则为text类型字段。String类型可以是枚举、外键，注意外键和枚举字段名建议以Id结束。注意： List类型通常不需要创建，系统会根据外键关系定义自动生成，只有当是在一个领域应用中的对象，如果要关联另一个领域应用的多态参照对象时，需要手工建立list属性关系。
length | integer | 长度 | 否 | 只有字符型可以指定长度，不指定表示text类型，枚举和ID类型长度长度固定，不需要指定
enumType | string | 枚举类型名称 | 否 | 只有字段类型是String时，可以指定枚举类型名称，以标识为枚举字段。枚举字段，会自动建立对EnumValue对象的参考关系
	bizType | string | 字段的业务类型标识 | 否 | 具有特定的业务含义的字段标识，如果quantity, amount, email等
	expression | string | 计算表达式 | 否 | 如果有表达式，表示这是一个需要自动计算的字段，通常不能直接编辑， 通过表达式运算得到值。
	writeBackExpr | object | 回写表达式 | 否 | 如果有值，表示这是一个需要其他对象变更时回写的表达式字段。
	triggerExpr | string | trigger表达式 | 否 | 如果有表达式，表示这是一个DB trigger触发的表达式。
	isCustomField | boolean | 是否自定义字段 | 否 | 默认值为false
isSystemField | boolean | 是否系统字段 | 否 | 统字段通常为自动维护值的字段，一般不允许前端手工修改，默认为false
isDisabled | boolean | 字段是否被停用 | 否 | 系统停用和用户停用，都使用该标识，只不过用户停用时，是保存在自定义元数据表中。 默认为false
isFlowField | boolean | 字段是否流程使用字段 | 否 | 当单据支持变更单时，指定了流程使用的字段后，对应生成的流程使用字段，通常为Immutable结尾。生成的Immutable字段isFlowField也为true
isHisVerId | boolean | 是否为多版本外键|否 | 是否为数据多版本的外键, 由外键字段自动生成
referInfo | ReferInfo | 参照信息 | 否 | 如果是字符类型字段，并且存在参照关系，即是外键字段，或者多态外键字段，则必须指定referInfo信息，为null表示普通的string字段。详细说明参考 ReferInfo构成
sourceInfo | SourceInfo | 属性来源信息 | 否 | 通常不需要填写，根据referInfo自动生成。表示List、多态外键对象属性或者反向外键属性需要指明来源的对象和字段等信息。如果是跨领域应用手式创建的List属性，则需要提供 sourceInfo。 详细说明参考 SourceInfo构成。
properties | object | 字段的其他属性 | 否 | 对象的其他元数据说明，为一个map结构，根据具体的场景填写值，具体支持的proerties定义，参考 FieldProperties.java 中的定义
	
###3.2.2 ReferInfo构成
如果是有外键参照关系，保持多态外键，需要使用ReferInfo来描述：
	
属性 | 类型(json) | 中文名称 | 必填 | 说明
----|----|---- | ---- | ----
referEntityFieldName | string | 多态参照的对象类型字段 | 否 | 仅用于多态参照，该属性指定了保存了参照实体名称的字段名，该字段必须是当前实体对象的一个字符型字段。默认为null, 表示为普通外键参照关系
isGenObjectField | boolean | 多态参照时是否生成外键对象字段 | 否 | 仅用于多态参照，表示为在当前的对象的POJO中，为多态参照的每一个参照对象，生成一个外键对象属性及元数据，相应的id字段对应的参照对象属性规则为： abc 或者 abcId -> abc<参照对象名>。多态引用生成的外键字段元数据与普通外键字段元数据的区别是referEntityFieldName有值, 并且属性名以参照对象名为后缀, 并且有sourceInfo指向原多态参照id字段。 默认值为false
referEntities | array | 参照实体信息 | 是 |  如果是多态参照，则可以用多个值指定可以参照的本领域应用中的对象类型， 对于跨领域的多态参照关系，由于元数据有一定的隔离，不能改变其他领域的对象的字段定义，因此由被参照对象手工定义list属性实现多态参照。对于普通的外键参照，通常只有一个值指定要参照的本领域或者依赖的领域的对象类型。无论是多态参照，还是普通外键参照，如果要定义反向参照，且反向参照不止一个时（比如地址信息多态参照订单，则可以用有两个反向参照收货地址和发票地址），则可以增加一个ReferEntity值来单独说明反向参照关系。具体值的说明参考 ReferEntity构成
	
###3.2.3 ReferEntity构成
ReferEntity描述了参照的对象类型，及是否要在被参照对象（目标)上生成列表属性（Listn属性)和反向关联属性(反向FK属性)元数据。如果目标对象是本领域，则List属性和反向FK属性会影响生成的java pojo对象， 否则只影响到元数据，最终在Graphql查询时起作用。
	
属性 | 类型(json) | 中文名称 | 必填 | 说明
----|----|---- | ---- | ----
referEntityName | string | 被参照对象名称 | 是 
listFieldName | string | 列表属性名称 | 否 | 要在被参照对象的POJO和GraphQL查询Schema上生成当前对象的列表属性的名称及对应元数据，不指定表示不生成列表属性
listFieldTitle | string | 列表属性标题 | 否 | 要生成的列表属性的标题，当listFieldName存在时有效
isDetail | boolean | 是否为主子关系 | 否 | 如果是主子关系，表示与主对象同时CUD操作，在列表属性时有效，即主子关系必须有列表属性, 同时当前对象必须为明细对象，否则没有明细属性保存主子对象会报错。默认为false
referedFieldName | string | 反向外键属性 | 否 | 即在被参照的对象的POJO和GraphQL查询Schema中，生成一个外键对象属性指向当前对象, 生成的反向外键元数据与普通外键元数据的区别是，反向外键有sourceInfo标识反向的参照关系。通常两个对象是1:1关系，需要指定等价约束条件。
referedFieldTitle | string | 反向外键属性标题 | 否 | 要生成的反向外键属性的标题，当referedFieldName存在时有效
referedCondition | string | 等价约束条件 | 否 | 要生成的反向外键属性的等价约束条件，当referedFieldName存在时有效。约束条件为标准的类sql条件表达式中，只能出现当前实体的字段名, 并且字段名必须加上 `${alias}.` 前缀。

>注意： 对于isDetail为true的情况，要求指向的主对象的服务中，必须可以访问当前对象的repository对象，否则主子表连带操作会报错。如果当前对象不是明细对象，也可以做为子列表，这时子列表的数据有ID即为更新，没ID为新增.

###3.2.4 SourceInfo构成
SourceInfo通常是List属性、多态外键的对象字段和反向FK属性需要提供，用来描述该属性的来源属性。通常有由来源属性的元数据自动生成 List和反向FK属性，同时SourceInfo也自动生成，不需要定义。
如果是直接在对象上建立list类型的属性，则需要手工维护sourceInfo，指定来源字段为一个多态外键，或者一个指向自己的普通外键字段。对于跨应用的情况，通常只有多态外键可以用于建立list属性。此外list属性是否可以为子表(isDetail=true), 取决于List的对象的Repository是否可以访问。

属性 | 类型(json) | 中文名称 | 必填 | 说明
----|----|---- | ---- | ----
sourceEntityName | string | 来源对象名称 | 是 
sourceFieldName | string | 来源外键字段名称 | 是
referedCondition | string | 等价约束条件 | 否 | 反向外键属性时，等于来源字段中的 referedCondition 值
isDetail | boolean | 是否为明细列表 | 否 | 对于列表属性时，用来说明是子列表有关系，通常与来源字段的 isDetail 相同。默认为false
	
###3.2.5 bizType类型说明
目前支持的bizType有以下几种：

类型 |  中文名称 |  说明
----| ---- | ----
mobile | 手机 | 表示可以发送短信
phone | 电话 | 表示可以播号
email | 电子邮件 | 表示为一个邮件地址
quantity | 数量 | 表示数值为一个数量
price | 价格
amount | 金额 | 表示为一个金额字段，当附加的properties中有指定 exchangeRateField或者taxRateField时，要生成相应的外币和税额等字段
ratio | 比率
percent | 百分比 | 要显示为百分数
	
##3.3 枚举元数据定义
Entity中的字段，如果是一个枚举值，实际上可以看做是一个普通的外键参照字段，该字段的被参照对象为EnumValue对象，只不来EnumValue对象中数据来源于枚举元数据，因此定义的枚举元数据会自动插入到EnumValue表中。

###3.3.1 枚举类型定义
	
属性 | 类型(json) | 中文名称 | 必填 | 说明
----|----|---- | ---- | ----
name | string | 类型名称 | 是 
title | string | 类型标题 | 是
desc | string | 类型说明 | 否
isCutomizable | boolean | 是否可以自定义 | 否 | 如果不可以自定义，表示用户不能增加或者停用枚举值
enumValueDefs | array | 包含的枚举值 | 是 | 枚举类型包含的枚举值
	
###3.3.2 枚举值定义

属性 | 类型(json) | 中文名称 | 必填 | 说明
----|----|---- | ---- | ----
value | string | 枚举值编码 | 是 
title | string | 枚举显示值 | 是
ordinal | integer | 序号 | 否 | 顺序编号
isCutomizable | boolean | 是否自定义 | 否 | 如果不可以自定义检举
isDisabled | boolean | 是否停用 | 否 | 枚举值是否停用，开发态可以设置停用，用户态自定义也可以设置。 默认false
isHidden | boolean | 是否隐藏 | 否 | 如果有的枚举值，属于中间状态，不希望用户查看，可以设置为隐藏
	
##3.4 计算字段/表达式字段说明
计算字段，也叫表达式字段， 它的值是根据一个表达式进行计算得到，除此之外，与普通字段没有区别。

###3.4.1 计算字段的约定

- 目前仅支持数值类的字段可以定义计算字段。理论上所有类型都可以定义为计算字段。
- 一个字段指定了expression，便自动成为计算字段，字段的值对象新建保存和修改保存的时候进行计算，因此前端计算结果是参考，以后端计算结果为准。
- 所有的计算字段都会持久化存储， 保存之后，后继的使用与普通字段一致。
- 为了前端能够联动计算，需要有前后端都认识的表达式语法
- 关于计算字段中涉及的字段的值变化时，是否重新计算的问题，目前的规则如下：
	- 仅本对象的值变化，才进行重算（因为计算的值应该与当时的现状有关系，比如客户级别的变化并不要求重算历史的单据）。即右计算字段所在的对象保存时才会计算，相关的其他对象的改动，不影响计算字段的值。
	-  对于客户级别变化这种变量，如果要重新计算，可以采取带入级别到单据上，定义表达式时，取决于使用的引用字段，还是本对象字段，决定是否重算）
	-  对于单据之间的发生数量，不采取自定义表达式字段，采取专用的存量模型进行。因此sum等聚合运算，只允许出现在主子表对象上，且只支持简单的表式

###3.4.2 计算表达式的定义说明

一个计算字段的计算表达式，通常由运算符、函数、常量和变量构成， 语法规则参考基准语言 java/javascript表达式,  其中：

- 运算符分为逻辑运算和数据运算等， 规则参考基准语言
- 常量规则参考基准语言
- 函数支持基准语言的标准函数外，还支持自定义扩展函数
- 变量分为以下三种：
	- 计算字段所在的对象上的其他字段名，支持级联字段， 比如对于SalesOrder对象，以下变量是合法的：
		- name
		- customerId
		- customer.type
	- 计算字段所在的对象的子表字段，仅限于一些特殊的聚合运算函数中使用, 它基于当前的子表字段，  比如对于SalesOrder对象的items子表，以下变量是合法的：
		- amount
		- product.type
	- 当前的环境变量，目前支持以下几种：
		- __currentDate 当前日期  
		- __currentUserId 当前用户
		- __currentDeptId 当前用户所在部门

###3.4.3 表达式支持的运符

类别 | 运算符 |  举例
----|----|---- 
一元数学运算符 | +， - |  -5, -abc
一元逻辑运算符 | ! |  !(a>0)
二元数学运算符 | +， -， *， /, % | a+b*c
二元比较运算符 | ==, !=, >, <, >=, <= | a!=b 
二元逻辑运算符 | &&, \|\| | a>0 || b>0
三元条件运算符 | ?, : | a>b?c:d
改变运算优先级 | (, ) | a*(b+c)
函数 |  (, ,, } | sumc(a, 'b', 'c>0'), sum(a, 'b*2')
字段级联 | . | a.b.c 
	
###3.4.4 表达式支持的自定义函数

####3.4.4.1 通用运算函数	

函数 | 中文名称 |  返回值类型 | 参数说明  | 举例
----|----|---- | ---- | ----
ifnull(&lt;orgValue>, $lt;defaultValue>） | 空值返回默认值 | 原始数据类型 |  orgValue: 原值<br> defaultValue: 原值为空时返回的值 | ifnull(a, 0)


####3.4.4.2 用于当前对象子表（List字段）的运算函数

函数 | 中文名称 |  返回值类型 | 参数说明  | 举例
----|----|---- | ---- | ----
sum(&lt;listFieldName>, &lt;expression>) |  汇总 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式 |  sum(items, 'amount*2')
sumc(&lt;listFieldName>, &lt;expression>, &lt;condition>) |  条件汇总 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>condition 基于子表的过滤条件表达式 |  sum(items, 'amount*2', 'amount>5')
avg(&lt;listFieldName>, &lt;expression>) |  平均 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式 |  avg(items, 'amount*2')
avgc(&lt;listFieldName>, &lt;expression>, &lt;condition>) |  条件平均 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>condition 基于子表的过滤条件表达式 |  avgc(items, 'amount*2', 'amount>5')	
max(&lt;listFieldName>, &lt;expression>) |  最大值 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式 |  max(items, 'amount*2')
maxc(&lt;listFieldName>, &lt;expression>, &lt;condition>) |  条件最大值 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>condition 基于子表的过滤条件表达式 |  maxc(items, 'amount*2', 'amount>5')
min(&lt;listFieldName>, &lt;expression>) |  最小值 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式 |  min(items, 'amount*2')
minc(&lt;listFieldName>, &lt;expression>, &lt;condition>) |  条件最小值 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>condition 基于子表的过滤条件表达式 |  minc(items, 'amount*2', 'amount>5')
first(&lt;listFieldName>, &lt;expression>) |  第一行的值 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式 |  first(items, 'productId')
firstc(&lt;listFieldName>, &lt;expression>, &lt;condition>) |  条件第一行的值 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>condition 基于子表的过滤条件表达式 |  first(items, 'productId', 'amount>5')
last(&lt;listFieldName>, &lt;expression>) |  最后一行的值| 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式 |  last(items, 'productId')
lastc(&lt;listFieldName>, &lt;expression>, &lt;condition>) |  条件最后一行的值 | 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>condition 基于子表的过滤条件表达式 |  lastc(items, 'productId', 'amount>5')
stragg(&lt;listFieldName>, &lt;expression>, &lt;delimiter>) |  字符串合并| 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>delimiter: 分隔符 |  stragg(items, 'descrition', ',')
straggc(&lt;listFieldName>, &lt;expression>, &lt;delimiter>, &lt;condition>) |  条件字符串合并| 原始数据类型 |  listFieldName: 子表字段名<br>expression: 子表表达式<br>delimiter: 分隔符<br>condition：基于子表的过滤条件表达式 |  stragg(items, 'descrition', ',')
count(&lt;listFieldName>>) |  计数统计 | Integer |  listFieldName: 子表字段名<br> |  count(items)
countc(&lt;listFieldName>, &lt;condition>) |  条件计数 | Integer |  listFieldName: 子表字段名<br>condition 基于子表的过滤条件表达式 |  countc(items, 'amount>5')

###3.4.5 计算字段的实现
1. 在新增或者保存对象完成后处理
2. 对于新增，所有的计算字段都需要重新计算
3. 对于修改，只计算改变后的字段值影响的计算字段才重新计算
4. 计算完成后，与对象中的计算字段值对比（有可能前端已经计算），如果值没有变化（decimal判断精度大小小数后8位），则不更新
5. 计算字段可能依赖，因此需要先算被依赖的。目前暂不支持子表计算时，依赖的主表字段也是计算字段（主表可以依赖子表的计算字段）。
6. 如果子对象的计算字段关联有主表字段变化，则该子表的所有记录都要重算，而不只是更新的子表记录。

##3.5 回写表达式字段(writeBackExpr)

回写表达式字段是指下游对象改变时，需要回写值到上游对象上的字段。回写表达式字段与普通表达式字段的主要区别是，回写表达式字段是下游对象改变时才计算的字段，普通表达式字段是指本对象改变时要计算的字段。由于计算的时机不同，因此回写表达式字段要声明以下属性：

- srcObjectType: 必填， 下游对象名称
- idField: 必填，基于下游对象的指向本对象的id的字段名（支持级联a.b.c)
- expression: 必填，基于下游对象的聚合运算表达式，注意因为要对idField进行groupby, 因此必须是聚合表达式, 且这个表达式的规则和普通表达式不一样，它是类型于GQL支持的聚合表达式（支持DB的函数)
- condition: 基于下游对象的过滤条件，它是类型于GQL支持的条件表达式（支持DB的函数)

实现方式： 在服务的CUD方法后，加上writeBackExpr 的Feature处理机制，在本对象改变时，查询所有的涉及的上游对象，计算值并更新上游对象。

注意：

- 回写过程与原对象更新在一个事务中处理
- 回写更改的对象暂不发出对象更新消息
- 一个字段同时可能是普通计算字段和回写表达式字段，其计算结果以最后一次触发计算条件为准
- 回写表达式字段不能参加到其它表达式的计算过程中
- 自定义字段也支持回写

##3.6 trigger表达式
trigger表达式是第三种计算字段， 它的表达式仅有本表的字段和常量构成，计算时机是在sql执行时，由trigger触发。它的表达式语法为SQL语法。

三类计算字段的比较如下：

名称 | 语法 | 变量基于对象 | 计算时机 | 其它说明 
---|---|---|---|----
普通计算字段 |  MVEL + 增强的聚合函数 | 当前对象及子对象的相关字段及级联字段 | 当前对象更新成功后，事务提交前 | 涉及的字段只要符合MEVL语法，并且变量可以级联，但不能包括其回写字段和trigger字段
回写表达式字段 | 基于SQL的EQL语法 | 基于指定的对象的字段及级联字段 | 其它对象变更成功后，事务提交前 |  表达式中可以包括指定普通计算字段，但不能包括其他回写字段
trigger表达式字段 | SQL语法 | 仅本对象的字段 | 当前对象的SQL执行时 | 表达式中可以是当前对象的任意字段，但不包括级联字段，不包括其他trigger字段（在字段定义顺序后面的trigger字段可以包括前面的trigger字段） 

注： 如果一个字段是trigger表达式，则不能是普通表达式、回写表达式同时定义。

trigger表达式字段定义为： 在field中增加表达式属性：
triggerExpr: "SQL语法"

##3.7 virtualField 虚拟字段

- 只定义title, 类型和virtualExpr表达式，virutalExpr如果指定，表示该字段是一个虚拟字段，pojo及物理表都不会生成该字段，只有元数据中有该字段的定义，仅用于gql查询。
- virutalExpr的语法为基于SQL的EQL语法。
- 通常表达式字段中， 字段不需要别名，表示当前查询对象的字段名为别名，如果特殊的情况（比如用到了子查询），需要区分当前查询对象的字段和子表字段时，要在当前查询的字段上加上别名${alias}前缀, 表示当前查询的表的字段，子表的不用加。比如： viurtalExpr="select string_agg(name, ',') from Contact where customerId=${alias}.id"

#4 元数据定义举例说明
##4.1 普通实体
###4.1.1 元数据定义
	
```json
{
  "name": "Role",
  "fields": [
    {
      "name": "code",
      "title": "编码",
      "type": "String",
      "length": 128,
      "referInfo": {
        "referEntities": [
          {
            "referEntityName": "User"
          }
        ]
      }
    },
    {
      "name": "name",
      "title": "角色名称",
      "type": "String",
      "length": 128
    },
    {
      "name": "description",
      "title": "说明",
      "type": "String"
    }
  ],
  "title": "角色",
  "desc": "用户的角色信息"
}
```

###4.1.2 生成的Entity Interface
目前所有的Entity采用了Immutables对象，因此只需要生成相应的接口即可：

```
@Gson.TypeAdapters
@Value.Immutable
public interface Role extends BaseEntity {
    @FieldMeta(title="编码",length=64)
    Optional<String> getCode();

    @FieldMeta(title="角色名称",length=128)
    Optional<String> getName();

    @FieldMeta(title="说明",length=2147483647)
    Optional<String> getDescription();

    @FieldMeta(title="编码")
    Optional<User> getCodeObject();

    @FieldMeta(title="创建人")
    Optional<User> getCreatedUser();

    @FieldMeta(title="修改人")
    Optional<User> getModifiedUser();

    @FieldMeta(title="用户")
    List<UserRole> getUsers();

    public static interface Builder extends BaseEntityBuilder {
    }
}
```

其中 BaseEntity为：

```
public interface BaseEntity extends Base {
  @FieldMeta(ordinal=0, title = "ID", length = 64)
  Optional<String> getId();
  @FieldMeta(ordinal=1, title = "创建人")
  Optional<String> getCreatedUserId();
  @FieldMeta(ordinal=2, title = "创建时间")
  Optional<Timestamp> getCreatedTime();
  @FieldMeta(ordinal=3, title = "修改人")
  Optional<String> getModifiedUserId();
  @FieldMeta(ordinal=4, title = "修改时间")
  Optional<Timestamp> getModifiedTime();
  @FieldMeta(ordinal=5, title = "是否为初始数据")
  Optional<Boolean> getIsInitData();
  @FieldMeta(ordinal=6, title = "是否禁止用该初始数据")
  Optional<Boolean> getIsInitDisabled();
  @FieldMeta(ordinal=7, title = "数据版本")
  Optional<Long> getDataVersion();
  
  @FieldMeta(ordinal=8, title = "自定义属性")
  Optional<JsonObject> getCustomizedFields();
  
  @FieldMeta(title = "需要将值设置为null的字段")
  @Ignore
  public Set<String> getToNullFields();

  @FieldMeta(title = "附加的值")
  @Ignore
  public Map<String, Object> getAdditionalValues();
}
```

###4.1.3 生成的表结构脚本
```
DROP TABLE IF EXISTS "role" CASCADE;
CREATE TABLE "role"
(
    "id" VARCHAR(64),
    "code" VARCHAR(64),
    "name" VARCHAR(128),
    "description" TEXT,
    "created_user_id" VARCHAR(64),
    "created_time" TIMESTAMP,
    "modified_user_id" VARCHAR(64),
    "modified_time" TIMESTAMP,
    "is_init_data" BOOLEAN DEFAULT false,
    "is_init_disabled" BOOLEAN DEFAULT false,
    "data_version" BIGINT DEFAULT 0,
    "customized_fields" JSONB,
    PRIMARY KEY ("id")
);
COMMENT ON COLUMN "role"."code" IS '编码';
COMMENT ON COLUMN "role"."name" IS '角色名称';
COMMENT ON COLUMN "role"."description" IS '说明';
COMMENT ON COLUMN "role"."created_user_id" IS '创建人';
COMMENT ON COLUMN "role"."created_time" IS '创建时间';
COMMENT ON COLUMN "role"."modified_user_id" IS '修改人';
COMMENT ON COLUMN "role"."modified_time" IS '修改时间';
COMMENT ON COLUMN "role"."is_init_data" IS '是否为初始数据';
COMMENT ON COLUMN "role"."is_init_disabled" IS '是否禁止用该初始数据';
COMMENT ON COLUMN "role"."data_version" IS '数据版本';
COMMENT ON COLUMN "role"."customized_fields" IS '自定义属性';
COMMENT ON TABLE "role" IS '角色';
```

##4.2 树型对象
###4.2.1 元数据定义

```
{
  "name": "ProductCategory",
  "fields": [
    {
      "name": "code",
      "title": "编码",
      "type": "String",
      "length": 64
    },
    {
      "name": "name",
      "title": "名称",
      "type": "String",
      "length": 256
    }
  ],
  "title": "商品分类",
  "isTree": true
}
```

###4.2.2 生成的Entity Interface

```
@Gson.TypeAdapters
@Value.Immutable
public interface CustomerCategory extends BaseTreeEntity {
    @FieldMeta(title="编码",length=64)
    Optional<String> getCode();

    @FieldMeta(title="名称",length=256)
    Optional<String> getName();

    @FieldMeta(title="创建人")
    Optional<User> getCreatedUser();

    @FieldMeta(title="修改人")
    Optional<User> getModifiedUser();

    @FieldMeta(title="上级")
    Optional<CustomerCategory> getParent();

    @FieldMeta(title="下级")
    List<CustomerCategory> getChildren();

    @FieldMeta(title="客户列表")
    List<Customer> getCustomers();

    public static interface Builder extends BaseTreeEntityBuilder {
    }
}
```

###4.2.3 生成的表结构脚本

```
DROP TABLE IF EXISTS "customer_category" CASCADE;
CREATE TABLE "customer_category"
(
    "id" VARCHAR(64),
    "code" VARCHAR(64),
    "name" VARCHAR(256),
    "created_user_id" VARCHAR(64),
    "created_time" TIMESTAMP,
    "modified_user_id" VARCHAR(64),
    "modified_time" TIMESTAMP,
    "is_init_data" BOOLEAN DEFAULT false,
    "is_init_disabled" BOOLEAN DEFAULT false,
    "data_version" BIGINT DEFAULT 0,
    "customized_fields" JSONB,
    "parent_id" VARCHAR(64),
    "id_path" VARCHAR(512),
    "level" INTEGER DEFAULT 0,
    "is_leaf" BOOLEAN DEFAULT false,
    PRIMARY KEY ("id")
);
COMMENT ON COLUMN "customer_category"."code" IS '编码';
COMMENT ON COLUMN "customer_category"."name" IS '名称';
COMMENT ON COLUMN "customer_category"."created_user_id" IS '创建人';
COMMENT ON COLUMN "customer_category"."created_time" IS '创建时间';
COMMENT ON COLUMN "customer_category"."modified_user_id" IS '修改人';
COMMENT ON COLUMN "customer_category"."modified_time" IS '修改时间';
COMMENT ON COLUMN "customer_category"."is_init_data" IS '是否为初始数据';
COMMENT ON COLUMN "customer_category"."is_init_disabled" IS '是否禁止用该初始数据';
COMMENT ON COLUMN "customer_category"."data_version" IS '数据版本';
COMMENT ON COLUMN "customer_category"."customized_fields" IS '自定义属性';
COMMENT ON COLUMN "customer_category"."parent_id" IS '上级';
COMMENT ON COLUMN "customer_category"."id_path" IS 'ID Path';
COMMENT ON COLUMN "customer_category"."level" IS '级次';
COMMENT ON COLUMN "customer_category"."is_leaf" IS '是否叶级';
COMMENT ON TABLE "customer_category" IS '客户分类';
```
##4.3 多态外键参照和反向外键参照 
###4.3.1 元数据定义

多态参照

```
{
  "name": "Attachment",
  "title": "附件",
  "desc": "保存所有的附件信息",
  "isDetail": true,
  "fields": [
    {
      "name": "objectType",
      "title": "关联的对象类型名称",
      "type": "String",
      "length": 32,
      "customized": false
    },
    {
      "name": "objectId",
      "title": "关联的对象",
      "type": "String",
      "referInfo": {
        "referEntityFieldName": "objectType", 
        "isGenObjectField": true,
        "referEntities": [
          {
            "referEntityName": "Contact",
            "listFieldName": "attachments",
            "listFieldTitle": "附件列表",
            "isDetail": true
          },
          {
            "referEntityName": "Customer",
            "listFieldName": "attachments",
            "listFieldTitle": "附件列表",
            "isDetail": true
          }
        ]
      },
      "customized": false
    },
    ...
  ]
}
```

反向外键参照

```
{
  "name": "Contact",
  "title": "联系人",
  "fields": [
    ...,
    {
      "name": "isPrimary",
      "title": "主联系人",
      "type": "Boolean",
      "customized": false
    },
    {
      "name": "customerId",
      "title": "客户",
      "type": "String",
      "referInfo": {
        "referEntities": [
          {
            "referEntityName": "Customer",
            "listFieldName": "contacts",
            "listFieldTitle": "联系人列表",
            "isDetail": false
          },
          {
            "referEntityName": "Customer",
            "referedFieldName": "primaryContact",
            "referedFieldTitle": "主联系人",
            "referedCondition": "${alias}.isPrimary=true"
          }
        ]
      },
      "customized": false
    }
  ],
  "isDetail": false
}
```

###4.3.2 生成的Entity Interface

Attachment接口

```
@Gson.TypeAdapters
@Value.Immutable
public interface Attachment extends BaseDetailEntity {
    @FieldMeta(title="关联的对象类型名称",length=32)
    Optional<String> getObjectType();

    @FieldMeta(title="关联的对象",length=64)
    Optional<String> getObjectId();

    ...
    
    @FieldMeta(title="关联的对象: Contact")
    Optional<Contact> getObjectContact();

    @FieldMeta(title="关联的对象: Customer")
    Optional<Customer> getObjectCustomer();

    ...
}
```

Customer接口：

```
@Gson.TypeAdapters
@Value.Immutable
public interface Customer extends BaseEntity {
    ...
    
    @FieldMeta(title="附件列表")
    List<Attachment> getAttachments();

    ...

    @FieldMeta(title="联系人列表")
    List<Contact> getContacts();

    @FieldMeta(title="主联系人")
    Optional<Contact> getPrimaryContact();

    public static interface Builder extends BaseEntityBuilder {
    }
}
```

###4.3.3 生成的表结构脚本

略

##4.4 其它
主子表对象 、枚举类型和枚举字段 、跨领域应用的List属性等示例，参考具体的微服务即可。

#5 关于自定义元数据
所有的自定义信息保存在表 CustomizedMetadata 中，它的字段信息说明请参考该实体的定义。
**自定义的信息中，涉及的name和value等，如果没有指定，可以自动生成全局唯一的名称**
自定义接口的操作实际上是对该表进行的CRUD操作。目前可以进行的自定义元数据操作有：

##5.1 新建自定义对象
 新建一个自定义对象，自定义对象有业务类型（用于前端的业务交互模板），是否为树状等。具体的实现方案为：

- 在Baseapp应用中，有三个模板实体 CustomizedEntityTemplate、CustomizedDetailEntityTemplate 和 CustomizedTreeEntityTemplate, 分别对应标准的自定义对象, 子表自定义对象和树型自定义对象，对应的系统元数据中，有这三个模板的元数据。
- 自定义对象新建时，会将模板对应的表，复制为一个自定义对象对应的表，同时将模板元数据从系统元数据中，复制到自定义元数据中，并修改相应的对象名，title等属性。**自定义对外一旦创建，对象名称不可更改，对象只能停用**
- 对于主子表关系，通过外键引用的方式说明
- 自定义对象共享一套标准的CUD接口，Gql查询与普通对象一致（需要特别处理自定义对象，标准的GQL schema中不存在自定义对象）
- 同样自定义对象的共享POJO（模板类的），所有的自定义对象创建元数据在baseapp中，底层在生成SQL时，自动将表名更换为真正的表名:
  - sql parser时
  - 在BaseService中使用接的SQL时
  - SQL DAO Mapper中将表名做为参数
        -   在BaseEntity中增加一字段为customizedTableName, 用于保存对象的表名（GQL中会排除该字段）
        -   在保存对象时，会将customizedTableName会在controller层自动赋值(其他直接调用 service的地方，需要手工赋值）
        -   在生成的mapper文件中，将所有的模板表名，更换为#{customizedTableName}
- 所有以数据对象的className的地方，需要转换为实际的对象名：
  - BaseService
  - BaseRepository

>注意： 自定义对象上如果要建立自定义属性，与常规对象一样，需要添加一条自定义字段数据，不在修改自定义对象记录。

##5.2 新增自定义枚举类型和枚举值
- 新建一个自定义枚举类型
- 在一个枚举类型上新增自定义枚举值
- 修改系统枚举值的title和ordinal，或者修改停用标记

>注意： 
>
>- 对于枚举值的变化，除了在元数据中记录外，同时同步更新到EnumValue表中。
>-  如果对自定义枚举类型进行枚举值的CUD操作，直接在原来的自定义类型上进行，不新增加自定义数据记录。


##5.3 新建自定义字段
- 为一个存在的Entity（包括自定义对象）新增加自定义字段，除Json外的所有类类型都可以自定义，如果是外键类型，目前只能是普通外键，并且不会生成反向外键
- 修改Entity标准字段的title，或者修改停用标记

#6 其他说明

##6.1 关于字段的默认值
DB中除ID字段外，所有的字段都不是必填，默认值通常都是null, 但对于所有boolean类型，默认值为false, 数值类型，默认值为0，因些在某些统计，条件查询时，数值和boolean不需要考虑null值情况。

##6.2 数据物理删除和逻辑删除
一个对象是物理删除还是逻辑删除，是通过对象的元数据isPhysicalDeletion来声明的，该标志的不同，会影响mybatis的mapper文件中对delete方法的实现策略。

其中逻辑删除，只是打删除标记（isDeleted), 因此，所有isDeleted=true的记录在mybatis的查询时，会过滤掉，使用mybatis的delete方法，实际也只是打删除标记。

默认的select和update sql中，如果没有is_deleted=? 做为条件的字段，则会自动添加is_deleted=false的过滤条件。因此为了能够查询或者修改逻辑删除的数据，只需要在查询的条件中指定了isDeleted=?的条件即可。

逻辑删除的逻辑在mybatis层实现，对用户透明。逻辑删除的过滤在sql处理层实现，对用户透明。

> 注意：元数据相关的对象（不包括EnumValue表），因为它们的ID通常是固定的，通常它们被使用了只能停用，不能删除，但在可以删除的情况下进行的删除操作采取的是真删除，否则再新增可能会主键冲突。

##6.3 预置数据的删除和修改
对于预置数据的删除，如果允许用户删除，采取的也是逻辑删除的模式。
对于预置数据的修改，采取的是copyOnWrite模式，即修改预置数据时，实际上是将预置数据的ID增加后缀-disabled,并且标记为删除,然后复制一条记录，保留原来的ID。

修改变新增记录逻辑由Service层自动实现，对实际使用透明。

这样，可以实现升级时，可以保持预置数据的isDeleted状态，但可以升级其他字段。这样对用户修改过的数据没有影响，也可以实现用户恢复到预置状态的特性。

**注意:**
  
  - 对于有预置数据的对象，要求必须是逻辑删除。
  - 并且对于业务数据，必须使用Service方法进行修改或者删除，禁止使用sql直接对业务业务数据进行操作，以免修改预置数据，升级时会使修改失效。
  - <font color=red>目前不允许批量update和insert操作中涉及到预置数据，因为为了性能考虑，批量操作没有处理预置数据。</font>
  - 同样对于子表也不支持预置数据处理逻辑

##6.4 TODO 删除引用的数据
 1. 如果一个对象是物理删除的，在删除前必须检查是否被使用，如果被使用了，则不允许删除
 2. 如果是逻辑删除的对象，可以不进行删除引用检查，但这要求查询时，join表不能加上删除记录过滤条件
 3. 如果删除检查，是否在外键引用定义时，说明删除是否要进行引用检查?
 
##6.4 ID统一生成
除元数据外，所有的对象的ID都是在Repository层中调用ID服务生成字串类型的ID。前端传递的数据，如果是修改，则需要提供ID，如果是新增，则不需要提供ID。

##6.5 EnumValue表中的枚举值同步
EnumValue主要是为了实现枚举对象可以当做普通对象一样进行关联查询而增加的一个枚举值冗余表，它采取的是逻辑删除的模式，而EnumValue表的ID是由EnumType名称加上EnumValue的Value值构成，即ID是按某一规则不变的，因此在删除和再新增时，如果有同一个value，可能会造成ID冲突。因此对该表的删除操作（在允许删除的前提下）要特别处理：如果不是init数据，则直接删除，否则按逻辑删除处理。该特别处理的实现在 EnumValue的Service层中进行。

##6.6 应用元数据的装载和隔离
每一个微服务实际是一个spring boot的app, 一个app可以信赖另一个应用的app的api包，它启动时没有信赖关系。由于允许应用之间的依赖，元数据又是在每个app装载的，因此可以配置元数据的装载范围。目前有以下几种装载选项（可以在应用的application.properties中定义，也可以在环境变量中定义）：

 1. 装入指定目录下的entities及enums定义元数据（用于非app测试及maven插件执行）, 指定目录方式 ： 
       
        System.setProperty(MetadataLoader.PROP_METADATA_ENTITY_DIR,"../metadata-impl/src/test/resources/entities"); 
    
        或者通过配置 application.properties 中的对应值：
           app.metadata.entity.dir
 2. 装入当前的classpath:entities/\*.json及entities/enums/*.json中的元数据（app开发态及运行环境）
 3. 装入数据库中指定领域的元数据，同时保持元数据的同步更新（app开发态及运行环境), 装入的配置通过配置： 
   
        app.metadata.load.from=DB|RESOURCE
        app.metadata.load.domains=a,b,c 装入的领域，不指定表示所有领域（仅在从数据库装入时有效）
        默认情况为： RESOURCE, 表示从resource装入，如果指定了目录 ，同时装入目录下的元数据

##6.7 数据多版本实现
数据多版本，是一个数据对象，在变化时，不但要保存历史的数据，同时，其他对象参照引用该数据对象时，可以查看引用的历史数据情况。比如部门如果支持多版本，则在订单上引用部门时，但部门结构调整后，可以根据当前的状态来确认部门的关系，也可以根据单据创建时的状态来确认部门的关系。

###6.7.1 多版本支持定义
数据多版本的实现首先要在元数据上声明对象支持数据多版本（通常是档案对象需要支持数据多版本）， 即isMultiDataVersion = true， 之后，要在表 MultiDataVersionField声明该对象的哪些字段值变化时，需要保存为多版本数据（默认没有指定字段时，不生成多版本数据）。

当一个对象支持多版本时, 对应会在表结构中多一个hisVerId字段。 该字段的值为:  当前ID + "-" + 数据新建或者变更时的dataVersion值。

对于外键参照了数据多版本的对应，对应自动生成一个历史外键字段，该字段名为： HisVer + 原外键字段名(首字段大写)

###6.7.2 多版本对象变更说明
假设部门对象是实现多版本的，且多版本的变化字段是只有部门名称，数据如下：

id | code | name | dataVersion | hisVerId   | isDeleted
----|----| ----|----|---- | ----
did1 | code1 | dept1 | 0 | did1-0 | false
did2 | code2 | dept2 | 0 | did2-0 | fasle

订单表中引用部门的数据如下：

id | code | deptId | deptHisVerId  
----|----| ----|----
id1 | order1 | did1 | did1-0
id2 | order2 | did2 | did2-0

当部门表中，更新第一条记录的name和第二条记录的code时，只有第一条记录会产生历史记录，更改后的结果如下：

id | code | name | dataVersion | hisVerId   | isDeleted
----|----| ----|----|---- | ----
did1 | code1 | dept1-1 | 1 | did1-1 | false
did1-0 | code1 | dept1 | 0 | did1-0 | true
did2 | code2-1 | dept2 | 1 | did2-0 | false

第二条记录的hisVerId保持不变， 这时如果再修改第二条记录的name时，数据变为：

id | code | name | dataVersion | hisVerId   | isDeleted
----|----| ----|----|---- | ----
did1 | code1 | dept1-1 | 1 | did1-1 | false
did1-0 | code1 | dept1 | 0 | did1-0 | true
did2 | code2-1 | dept2-1 | 2 | did2-2 | false
did2-0 | code2-1 | dept2 | 1 | did2-0 | true

这里如果再增加一条订单，引用did2, 则订单表为：

id | code | deptId | hisVerDeptId  
----|----| ----|----
id1 | order1 | did1 | did1-0
id2 | order2 | did2 | did2-0
id3 | oerder3 | did2 | did2-2

基于订单表的数据，如果查看订单时，如果要按目前部门的现状态来查看， 则直接使用deptId外键join部门表的id即可。如果要按订单原来的部门状态来查看，则使用hisVerDeptId外键join部门的hisVerId即可。
>注意：  
>1. 在向数据库提交insert语句时hisVerId字段一定要为null，否则后期将会产生不可预期的错误;  
>2. 在向数据库提交update语句时一定不能更新hisVerId字段，否则后期将会产生不可预期的错误;


###6.7.3 多版本具体实现要点

- Entity元数据增加isMultiDataVersion标识
- Field元数据如果是外键， 增加 isHisVerId 标识
- 在元数据从文件装载时：
	- 自动为isMultiDataVersion=true的对象创建一个hisVerId字段
	- 引用了多版本的对象的外键，自动创建一个HisVerId外键字段及相应的对象字段与list字段
- 自定义对象的模板中，除明细外，自动有多版本字段
- 自定义外键字段如果引用的是多版本字段，要根据原外键定义，对应产生HisVerId字段的元数据
- 使用DB Insert触发器:
	- 自动更新多版本数据对象的hisVerId字段值
	- 自动更新引用了多版本的外键字段的hisVerId字段值
- 使用DB Update触发器（与copy_on_update合并）
	- 当多版本数据对象变化时，自动产生多版本数据（标记为删除状态）
	- 当普通数据中，有涉及多版本对象的外键变化时，要同步更新hisVerId外键。
- 修改GraphQL查询，支持多版本数据的hisVerId联查（包括外键，子列表联查）

注： 对于HisVer外键对应的字段或者值的命名规则
- 外键HisVerId字段名："hisVer" +  原外键字段名(首字段大写)
- listFieldName: "hisVer" +  原ListFieldName值(首字段大写)
- listFieldTitle: 原title + "(历史)" 

##6.8 单据支持变更单 TODO

目前流程机制，当一个单据当进行到某一步时，锁定了相应的字段后，将不允许修改，以保证单据的流程能够正常的执行下去。但实际的业务要求，单据还是可以修改的，只不过修改的数据有一些约束，为了解决这一问题，引入单据变更单的概念，变更单是对单据修改产生的一张新的单据，它有自己的处理流程，因此变更单理论上不直接影响原单据的流程执行（可以通过过设置单据的onHold状态，来间接影响）。但用户在查看单据时，还是希望从整个单据的角度来看一张变更后的单据。为了实现这一目标，我们定义了流程使用的字段，这些字段也是变更单的主要构成字段，当流程在执行过程中，将这些字段锁定不允许修改时，用户还是要修改的话，实现上是进行产生的变更单。用户修改的字段为原字段，为了保持流程使用的字段不变性，我们为每一个流程使用的字段生成了一个对应的Immutable字段，但流程锁定该字段后，该字段的值将不会与对应的字段变化。

###6.8.1 要点

- 开发态定义Entity是否支持变更单，如果支持，则产生变更单对象，变更单对象名为原对象名加上ChangeLog为后缀
- 开发态定义流程使用的字段：只有Entity支持变更单的，才可以定义流程使用字段。流程使用的字段都有一个Immutable字段, 两者的值通常情况下是一样的，只有Immutable流程字段被流程锁定为不可编辑时，Immutable字段的值不再随对应的流程字段变化
- 对于自定义对象，可以设置是否变更单，只有对象支持变更单的对象的自定义字段，可以设置为流程字段，对应也会有Immutable自定义字段
- 流程定义过程中，需要考虑流程使用的字段的标准值还是Immutable值
- 在单据修改时，当Immutable字段被锁定的情况下，如果对应的非Immutable字段变化了，则需要产生一个增量变更单
- 变更单有自己的流程定义，在变更单中，可以将其对应的主单据的明细加锁或者解锁onHold状态
- 当一个单据的明细行上有onHold状态时，对应的逻辑要关注这个状态，以决定是否可以对单据进行某些操作（具体有实现的代码确认是否要使用onHold状态。
- 当多次变更，产生多个onHold状态，只要有一个onHold状态存在，则该明细为onHold状态。当有一条子记录为onHold状态，则主对象也为onHold状态
- 变更单不能删除
- 单据变更操作，受存量控制，只能在可用量范围内进行变更，这样上游变更，不影响已经存在的下游单据的流程执行。
- 变更单通常用于主子表对象，因此主表本身的变更会产生变更单，子表的变化也会产生变更单 ，子表的多行产生在一个变更单上
- 变更单主对象上有指向原单据的外键srcId， 变更单的子表上有指向原子表的外键srcId和原单据主表的外键src<原子表指向主表的外键字段名>


###6.8.2 具体变更单的实现逻辑如下:

-  在字段元数据上定义isFlowField字段，对应产生变更单字段和Immutable字段
- 在Entity的字段元数据上定义isSupportChangeLog字段, 当为true时，产生变更单对象，该对象为名"<原对象名>ChangeLog", 包含的字段有：
	- srcId： 指向原对象的Id
	- src&lt;$ParentId>： 如果是子表对象，指向原单据的ID（即查询当前对象的所有外键字段，如果产生list字段，且是参照的isDetail=true时，同时参照的对象A的isSupportChangeLog为true时，为该字段创建一个src&lt;$ParentId>字段）
	- isOnHold： 是否锁定记录状态，变更单的主子表同时修改。
	- 其它isFlowField标记为true的字段,表达式字段不能设置isFlowField标志
	
	> 注意：1. 生成的变更单实体对象的isChangeLog为true,以此可以判断一个实体是否为变更单  
       2. 在添加一个实体时，不要指定isChangeLog属性
      
- 在元数据从文件装载时，上述对象及字段的元数据，之后安元数据创建ddl
- 在运行态自定义对象和字段时，相应生成自定义的元数据及相应的物理表
-在对象update时，在流程的字段变化时，且字段已经被锁定的情况下，产生一张变更单
- 在DB update Update触发器（与其他update触发器合并）中，实现当字段为流程的Immutable字段时，要根据情况，如果是自动锁定字段，且被锁定时，将不请允许修改（保持原值），如果没有锁定，则将会与原值一起变化（相等）
- 为流程服务提供一个修改onHold标记的api，一个一个变更单中的isOnHold状态（主子表一起修改）

问题: 对于xxxChangeLog（变更单的) srcId


##6.9 模型继承(Inherit)

模型支持继承，即如果一个模型从另一个模型继承，则具有父级模型的所有属性。由于父级模型有外键字段，或者有其他对象的外键指向父级模型，或者父级型本身是树型对象，有自己指向自己的情况， 在继承的模型上也会有相应的体现，因些要注意以下问题：

- 模型可以多级继承
- 在子级对象继承父级对象的所有字段，同时如果子级对象中定义的字段，字段名与父级对象相同时，表示覆盖，但覆盖的字段类型不能改变（值要兼容父级类型）
- 每级对象预置的数据是对上级可见的，但对下级不可见，因此预置数据尽量在下级（运行态建的对象也类似）
- 上级对象A中，如果有外键fk指向其他对象B，则：
  - 如果fk上有list字段 fkList
    - 表示在B中生成包括A的list字段 fkList<A>（A是B的子列表）
    - 同时A的下级对象A1中也会在B中生成一个包括A1的list字段: a1FkList<A1> , 以避免同名（在原来的List名前加上对象名）， 些时允许在A中重定义fk字段，重命名a1FkList字段名。
  - 如果A指向的对象是一个被继承的对象，它有子对象B1
    - fk实际在查询时，默认是基于B查询，但也可以使用多态语法@B1实现对B对象进行关联查询
- 对于对象自己指向自己的情况（树型对象）， 它的子对象也同样具有树型特性，则它的字段会覆盖父级对象
  - 在具体的一条记录时，一个对象如果有fk指向自己，则指向的数据必须和当前的数据一同一类数据（不能是子类或者上级类）， 这时要求被继承的对象上有一个dataObjectId字段，指示数据的类型。
- 外键关系中， 如果有多态，类似上一条处理
- 对于子级对象中定义的计算字段（包括trigger), 如果通过父对象修改数据，不会触发子对象的计算（virtualField因为不用保存，所以可以使用）。

###6.9.1 抽象模型实现

1. 实体元数据增加定义
  - 增加属性inheritFrom 可以用于定义继承, 目前仅支持一个对象
2. ddl Gen build工具修改
  - 普通对象优先build，继承对象最后build（按继承层级，低的先build)
  - 继承对象DDL生成Inherits语法（排除继承的上级对象的所有字段）
3.修改mbg plugin：
  - pojo如果是继承，生成的接口（mutable和immutable）也有继承关系。下级需要排除上级的所有字段
  - repo、service、controller保持原来的逻辑
4. 元数据装载
  - 缓存增加一个subEntities属性，记录继承的直接子对象的名称列表（自动记录， 包括跨域时自动记录）
  - 元数据装载时，普通对象优先加载
  - 如果对象有下级对象，加入到subEntities属性中（考虑跨域）
  - 下级自动包含上级的所有字段元数据，在加入上级的字段时，如果对象有外键指向自身，则加入到当前对象时，将指向改为指向自己。相应的List字段也保持同名不变。
  - 在A.fkB生成的list字段B.listA时，如果A有下级，在B上生成下级的List字段，比如a1ListA
  - 如果继承对象有同样的字段时，以继承对象为准 ，如果类型不匹配报错
  - 唯一索引也要从父级继承（如果子类定义重名的索引，则以子类为准)
  - 所有的对象，生成一个虚拟字段dataObjectId，它是计算得到的，指向ObjectType。
  - 上级的自定义字段在子对象也也会自动加上
5. EQL对于对于查询fk时，如果fk指向了被继承对象，则支持@语法查询继承对象(如果dataObjectId与@指定的对象不匹配，且@指定的对象指向了子表，则可能查询不到数据),例如：m.baseMonthlyClosing@MonthlyClosing.accoutingBookId。
6. Gql对fk指向中的被继承对象的子对象也生成一个fk字段(暂时不支持，目前使用 @表达式查询即可)
7. 保存树型对象时，检查parentId对象， 只能是同一对象类型的对象

